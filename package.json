{
  "name": "mcp-refinery",
  "version": "0.2.0",
  "description": "Agentic delivery system for self-improving MCP servers — zero external dependencies beyond MCP SDK",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-refinery": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "bundle": "node build.mjs",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}

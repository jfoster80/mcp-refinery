# MCP Refinery — Model Provider API Keys
# Set any of these to enable direct API calls for that provider.
# Without keys, models work in "prompt mode" — the agent processes prompts manually.

# Anthropic (Claude Opus, Sonnet, Haiku)
ANTHROPIC_API_KEY=

# OpenAI (GPT-4o, o3, GPT-4o-mini)
OPENAI_API_KEY=

# Google (Gemini Pro, Flash)
GOOGLE_AI_API_KEY=

# xAI (Grok)
XAI_API_KEY=

# Data storage path (default: ./data relative to working directory)
# REFINERY_DATA_PATH=./data

# Config file path (default: ./refinery.config.json)
# REFINERY_CONFIG=./refinery.config.json
